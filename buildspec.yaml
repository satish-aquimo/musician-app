version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install

  build:
    commands:
      #- npm run build

      # Additional build commands if needed

artifacts:
  files:
    - '**/*'
 # base-directory: dist
